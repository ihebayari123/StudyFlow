security:
  password_hashers:
    App\Entity\Utilisateur:
      algorithm: auto

  providers:
    app_user_provider:
      entity:
        class: App\Entity\Utilisateur
        property: email

  firewalls:
    dev:
      pattern: ^/(_(profiler|wdt)|css|images|js)/
      security: false

    main:
        lazy: true
        provider: app_user_provider
        custom_authenticator: App\Security\LoginFormAuthenticator   # <- this line is essential
        logout:
            path: app_logout
            target: app_login

  access_control:
    - { path: ^/login$, roles: PUBLIC_ACCESS }
    - { path: ^/logout$, roles: PUBLIC_ACCESS }
    - { path: ^/register, roles: PUBLIC_ACCESS }

    # Admin area – accessible to admins and teachers
    - { path: ^/admin, roles: [ROLE_ADMIN, ROLE_ENSEIGNANT] }

    # Teacher choice page – only teachers (after login)
    - { path: ^/login/success, roles: ROLE_ENSEIGNANT }

    # Home (front office) – accessible to students and teachers
    - { path: ^/home, roles: [ROLE_ETUDIANT, ROLE_ENSEIGNANT] }

    # All other paths require at least ROLE_USER (any authenticated user)
    - { path: ^/, roles: ROLE_USER }

when@test:
  security:
    password_hashers:
      # By default, password hashers are resource intensive and take time. This is
      # important to generate secure password hashes. In tests however, secure hashes
      # are not important, waste resources and increase test times. The following
      # reduces the work factor to the lowest possible values.
      Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
        algorithm: auto
        cost: 4 # Lowest possible value for bcrypt
        time_cost: 3 # Lowest possible value for argon
        memory_cost: 10 # Lowest possible value for argon
