{% set newOrder = order == 'ASC' ? 'DESC' : 'ASC' %}
<<<<<<< Updated upstream

<div class="admin-layout">
=======
{% extends 'admin/base_admin.html.twig' %}

{% block title %}Manage Questions - Admin{% endblock %}

{% block admin_content %}
<div class="admin-main-content">
    <div class="container-fluid mt-5">
>>>>>>> Stashed changes

        <!-- Header -->
        <div class="page-header mb-4">
            <div class="row align-items-center">
                <div class="col">
                    <h2 class="page-title">
                        <i class="fas fa-question"></i> Manage Questions
                    </h2>
                    <p class="text-muted">View and manage all quiz questions</p>
                </div>
                <div class="col-auto">
                    <a href="{{ path('app_addformquestion') }}" class="btn btn-success">
                        <i class="fas fa-plus-circle"></i> Add Question
                    </a>
                </div>
            </div>
        </div>

        <!-- Search -->
        <div class="card mb-4">
            <div class="card-body">
                <form method="get">
                    <div class="row">
                        <div class="col-md-6">
                            <input type="text"
                                   name="q"
                                   class="form-control"
                                   placeholder="Search by text or choices..."
                                   value="{{ app.request.get('q') }}">
                        </div>
                    </div>
                </form>
            </div>
        </div>

        <!-- Table -->
        <div class="card shadow-sm">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead class="table-light">
                        <tr>
                            {% for column, label in {
                                'id':'ID',
                                'texte':'Text',
                                'choixA':'Choice A',
                                'choixB':'Choice B',
                                'choixC':'Choice C',
                                'choixD':'Choice D',
                                'bonneReponse':'Correct',
                                'indice':'Hint',
                                'quiz':'Quiz'
                            } %}
                                <th>
                                    {% if column != 'quiz' %}
                                        <a class="sort-link"
                                           href="{{ path('app_showquestion', {
                                                q: app.request.get('q'),
                                                sort: column,
                                                order: newOrder
                                           }) }}">
                                            {{ label }}
                                            <i class="fas fa-sort"></i>
                                        </a>
                                    {% else %}
                                        {{ label }}
                                    {% endif %}
                                </th>
                            {% endfor %}
                            <th>Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for question in listQuestion %}
                            <tr>
                                <td>{{ question.id }}</td>
                                <td>{{ question.texte|slice(0,40) }}{% if question.texte|length > 40 %}…{% endif %}</td>
                                <td>{{ question.choixA }}</td>
                                <td>{{ question.choixB }}</td>
                                <td>{{ question.choixC }}</td>
                                <td>{{ question.choixD }}</td>
                                <td>
                                    <span class="badge bg-success">
                                        {{ question.bonneReponse }}
                                    </span>
                                </td>
                                <td>{{ question.indice ?: 'N/A' }}</td>
                                <td>
                                    <span class="badge bg-info">
                                        {{ question.quiz ? question.quiz.titre : 'N/A' }}
                                    </span>
                                </td>
                                <td>
                                    <div class="btn-group">
                                        <a href="{{ path('app_updateformquestion', {'id': question.id}) }}"
                                           class="btn btn-sm btn-warning"
                                           title="Edit">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <a href="{{ path('app_deletequestion', {'id': question.id}) }}"
                                           class="btn btn-sm btn-danger"
                                           title="Delete"
                                           onclick="return confirm('Delete this question?')">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="10" class="text-center text-muted py-4">
                                    <i class="fas fa-inbox fa-3x mb-3"></i>
                                    <p>No questions found</p>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

    </div>
</div>

<style>
.admin-main-content {
    margin-left: 280px;
    padding-top: 100px;
}

.page-title {
    font-weight: 700;
    color: #2c3e50;
}

.page-title i {
    color: #667eea;
    margin-right: 10px;
}

.card {
    border-radius: 10px;
    border: none;
}

.table thead th {
    font-weight: 600;
    text-transform: uppercase;
    font-size: 0.8rem;
}

.table tbody tr:hover {
    background-color: #f0f4ff;
    transform: scale(1.01);
    box-shadow: 0 2px 8px rgba(102,126,234,0.15);
}

.sort-link {
    color: inherit;
    text-decoration: none;
}

.sort-link i {
    font-size: 0.7rem;
    margin-left: 5px;
    color: #adb5bd;
}

.btn-group {
    display: flex;
    gap: 5px;
}

@media (max-width: 768px) {
    .admin-main-content {
        margin-left: 0;
        padding-top: 80px;
    }
}
</style>

<link rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
{% endblock %}
<<<<<<< Updated upstream

{% block javascripts %}
    
    <script src="{{ asset('assets2/vendors/js/vendors.min.js') }}"></script>
    <script src="{{ asset('assets2/vendors/js/daterangepicker.min.js') }}"></script>
    <script src="{{ asset('assets2/vendors/js/apexcharts.min.js') }}"></script>
    <script src="{{ asset('assets2/vendors/js/circle-progress.min.js') }}"></script>
    <script src="{{ asset('assets2/js/common-init.min.js') }}"></script>
    <script src="{{ asset('assets2/js/dashboard-init.min.js') }}"></script>
    {% block admin_javascripts %}{% endblock %}
    <script src="{{ asset('assets2/js/theme-customizer-init.min.js') }}"></script>
{% endblock %}
=======
>>>>>>> Stashed changes
